[package]
name = "axiom-guest"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "axiom-guest"
path = "src/main.rs"

[dependencies]
# No "guest" feature in risc0 v1.x — target detection is automatic
risc0-zkvm = "1.0"
serde = { workspace = true }
serde_json = { workspace = true }
