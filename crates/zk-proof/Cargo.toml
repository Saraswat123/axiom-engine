[package]
name = "axiom-zk-proof"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
# RISC Zero added in Phase 5 — requires separate toolchain setup
# risc0-zkvm = "1.0"
